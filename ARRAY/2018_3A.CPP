#include<iostream.h>
#include<conio.h>
#include<stdio.h>
void SumEO(int values[],int n)
{
	int i,so,se;
	for(i=0;i<n;i++)
	{
		if (value[i]%2==0)
			se=se+value[i];
		else
			so=so+value[i];
	}
	cout<<"\nSum of even values: "<<se;
	cout<<"\nSum of odd values: "<<so;
}
void main()
{
	clrscr();
	int values[]={25,20,22,21,53);
	sumEO(values,5);
	getch();
}