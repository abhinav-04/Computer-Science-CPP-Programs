#include<iostream.h>
#include<conio.h>
#include<stdio.h>
//here sorted part increases and unsorted part decreases
void insert_sort(int arr[],int sz)
{
	int i,j,k;
	arr[0]=int_min;
	for(i=1;i<=sz;i++)
	{
		l=arr[i];
		j=i-1;
		while(k<arr[j] )
		{
			arr[j+1]=arr[j];
			j--;
		}
		arr[j+1]=k;
	}
}
