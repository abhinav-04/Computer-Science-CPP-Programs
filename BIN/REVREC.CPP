//reversing a number using recurssion
#include<iostream.h>
#include<conio.h>
#include<stdio.h>
int rev=0;
int reverse(int n)
{
	if(n==0)
	retutn rev;
	else
	{
		rev=(rev*10)+(n%10);
		return rev;
		}
